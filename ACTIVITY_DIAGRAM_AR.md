# مخطط النشاط للعبة Bunny Carrot Adventure

يوضح هذا الملف تدفق نشاط اللاعب داخل اللعبة من لحظة فتحها حتى الفوز أو الخسارة أو الرجوع للقائمة. المخطط مبني على حلقة اللعب الحالية: اختيار المرحلة، بدء اللعب، جمع الجزر، تجنب المخاطر، ثم الانتقال إلى شاشة الفوز أو الخسارة.

## ملخص التدفق

1. يبدأ اللاعب من القائمة الرئيسية.
2. يمكنه فتح التعليمات أو الإعدادات أو اختيار شخصية الأرنب، ثم الرجوع للقائمة.
3. عند بدء اللعب ينتقل إلى خريطة المراحل.
4. يختار اللاعب مرحلة مفتوحة.
5. تبدأ المرحلة بتصفير النقاط، ضبط القلوب، تشغيل المؤقت، وتوليد الجزر والمخاطر والثعالب.
6. يتحرك اللاعب داخل الحديقة ويكرر الأفعال التالية حتى نهاية المرحلة:
   - جمع الجزر لزيادة النقاط ورصيد الجزر.
   - استخدام الاندفاع للهروب أو الوصول بسرعة.
   - تجنب الثعالب والحفر والشوك.
   - خسارة قلب عند الاصطدام بخطر.
7. إذا وصلت النقاط إلى المطلوب قبل نفاد القلوب أو الوقت، يفوز اللاعب وتفتح المرحلة التالية.
8. إذا نفدت القلوب أو انتهى الوقت قبل الوصول للهدف، تظهر شاشة الخسارة.
9. بعد الفوز أو الخسارة يمكن للاعب إعادة المرحلة، اختيار مرحلة أخرى، أو الرجوع للقائمة الرئيسية.

## مخطط نشاط بصيغة Mermaid

```mermaid
flowchart TD
    A([فتح اللعبة]) --> B[القائمة الرئيسية]

    B --> C{اختيار اللاعب}
    C -->|بدء اللعب / خريطة المراحل| D[عرض خريطة المراحل]
    C -->|اختيار الشخصية| E[عرض شخصيات الأرنب]
    C -->|التعليمات| F[عرض التعليمات]
    C -->|الإعدادات| G[تعديل الصوت / اللغة / الصعوبة]
    C -->|خروج| Z([إنهاء اللعبة])

    E --> B
    F --> B
    G --> B

    D --> H{هل المرحلة مفتوحة؟}
    H -->|لا| D
    H -->|نعم| I[اختيار المرحلة]

    I --> J[تهيئة المرحلة]
    J --> J1[تصفير النقاط وضبط القلوب والمؤقت]
    J1 --> J2[توليد الجزر والثعالب والحفر والشوك]
    J2 --> K[بدء اللعب]

    K --> L{حلقة اللعب مستمرة؟}
    L -->|نعم| M[تحريك الأرنب]
    M --> N{إجراء داخل المرحلة}

    N -->|جمع جزرة| O[زيادة النقاط ورصيد الجزر]
    O --> P{هل وصلت النقاط للهدف؟}
    P -->|نعم| W[إيقاف المؤقت وحفظ المكافآت]
    P -->|لا| Q{هل انتهى الوقت؟}

    N -->|استخدام الاندفاع| R[زيادة السرعة مؤقتا ثم انتظار إعادة الشحن]
    R --> Q

    N -->|اصطدام بثعلب أو حفرة أو شوك| S[إنقاص قلب وتشغيل حماية مؤقتة]
    S --> T{هل القلوب انتهت؟}
    T -->|نعم| X[إيقاف المؤقت وتشغيل الخسارة]
    T -->|لا| Q

    N -->|لا يوجد حدث خاص| Q

    Q -->|نعم والهدف غير مكتمل| X
    Q -->|لا| L

    W --> Y[فتح المرحلة التالية إن وجدت]
    Y --> AA[شاشة الفوز]
    AA --> AB{اختيار بعد الفوز}
    AB -->|المرحلة التالية| I
    AB -->|إعادة المرحلة| I
    AB -->|القائمة| B

    X --> AC[شاشة الخسارة]
    AC --> AD{اختيار بعد الخسارة}
    AD -->|إعادة المحاولة| I
    AD -->|خريطة المراحل| D
    AD -->|القائمة| B
```

## مخطط نشاط بصيغة PlantUML

يمكن نسخ الكود التالي إلى أي أداة PlantUML لعرضه كمخطط نشاط UML تقليدي.

```plantuml
@startuml
start
:فتح اللعبة;
:عرض القائمة الرئيسية;

if (اختيار اللاعب؟) then (بدء اللعب / خريطة المراحل)
  :عرض خريطة المراحل;
elseif (اختيار الشخصية) then
  :عرض شخصيات الأرنب;
  :الرجوع إلى القائمة الرئيسية;
elseif (التعليمات) then
  :عرض التعليمات;
  :الرجوع إلى القائمة الرئيسية;
elseif (الإعدادات) then
  :تعديل الصوت / اللغة / الصعوبة;
  :الرجوع إلى القائمة الرئيسية;
else (خروج)
  stop
endif

:اختيار مرحلة مفتوحة;
:تهيئة المرحلة;
:تصفير النقاط وضبط القلوب والمؤقت;
:توليد الجزر والثعالب والحفر والشوك;
:بدء اللعب;

repeat
  :تحريك الأرنب داخل الحديقة;

  if (جمع جزرة؟) then (نعم)
    :زيادة النقاط ورصيد الجزر;
    if (النقاط وصلت للهدف؟) then (نعم)
      :إيقاف المؤقت;
      :حفظ المكافآت;
      :فتح المرحلة التالية إن وجدت;
      :عرض شاشة الفوز;
      if (اختيار اللاعب بعد الفوز؟) then (المرحلة التالية)
        :بدء المرحلة التالية;
      elseif (إعادة المرحلة) then
        :إعادة تهيئة المرحلة الحالية;
      else (القائمة)
        :الرجوع إلى القائمة الرئيسية;
        stop
      endif
    endif
  elseif (استخدام الاندفاع؟) then (نعم)
    :زيادة السرعة مؤقتا;
    :انتظار إعادة شحن الاندفاع;
  elseif (اصطدام بخطر؟) then (نعم)
    :إنقاص قلب;
    :تشغيل حماية مؤقتة;
    if (القلوب انتهت؟) then (نعم)
      :إيقاف المؤقت;
      :عرض شاشة الخسارة;
      if (اختيار اللاعب بعد الخسارة؟) then (إعادة المحاولة)
        :إعادة تهيئة المرحلة الحالية;
      elseif (خريطة المراحل) then
        :عرض خريطة المراحل;
        stop
      else (القائمة)
        :الرجوع إلى القائمة الرئيسية;
        stop
      endif
    endif
  endif

repeat while (الوقت متبق والهدف غير مكتمل؟) is (نعم)

:إيقاف المؤقت;
:عرض شاشة الخسارة بسبب انتهاء الوقت;
stop
@enduml
```
